# input/draft/

Drop the drafted manuscript here as per-chapter Markdown files (one per chapter). The editor will archive these to `output_edit/original_draft_archive/` before any edits.

This directory in the shared package is empty by design — drop your draft chapters in.
